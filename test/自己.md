一文搞定箭头函数

本质是「匿名函数的语法糖」，核心作用是简化代码 + 稳定 this 指向。
箭头函数的 this 是如何绑定的？
this 是「继承自外层作用域的 this」，且在定义时就确定（词法绑定），之后永远无法修改（包括 call/apply/bind）
-浏览器环境：this 指向 window（全局对象）；
-Node 环境：this 指向 global（全局对象）；

一文搞定前端的this
